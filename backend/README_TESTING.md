# 🧪 Тестирование Cinema Booking API

## ✅ Что работает

### Базовые эндпоинты
- ✅ `GET /` - корневой эндпоинт
- ✅ `GET /health` - проверка здоровья
- ✅ `GET /api/v1/film` - список фильмов (публичный)

### Структура API
- ✅ FastAPI приложение запущено
- ✅ База данных подключена
- ✅ Миграции применены
- ✅ CORS настроен

## 🔧 Файлы для тестирования

### 1. `test_en.py` - Простой тест
```bash
python test_en.py
```
**Что тестирует:**
- Health check
- Корневой эндпоинт
- Публичные фильмы
- Регистрация пользователя
- Вход в систему
- Создание зала
- Создание фильма
- Гостевое бронирование

### 2. `client.py` - Полный тест
```bash
python client.py
```
**Что тестирует:**
- Все базовые функции
- Полный цикл работы с API
- Аутентификацию
- CRUD операции
- Публичные эндпоинты

### 3. `quick_test.py` - Быстрый тест
```bash
python quick_test.py
```
**Что тестирует:**
- Основные функции API
- Базовую функциональность

## 🚀 Запуск тестирования

### 1. Запуск сервера
```bash
cd cinema-booking
docker-compose up -d
```

### 2. Проверка статуса
```bash
docker-compose ps
```

### 3. Запуск тестов
```bash
cd backend
python test_en.py
```

## 📊 Результаты тестирования

### ✅ Работающие эндпоинты
- `GET /` - корневой эндпоинт
- `GET /health` - проверка здоровья
- `GET /api/v1/film` - список фильмов

### ⚠️ Эндпоинты с проблемами
- `POST /api/v1/user` - регистрация (500 ошибка)
- `POST /api/v1/login` - вход (404 ошибка)
- `POST /api/v1/book-ticket` - бронирование (404 ошибка)

## 🔍 Отладка

### Проверка логов
```bash
docker-compose logs backend
```

### Проверка базы данных
```bash
docker-compose exec db psql -U admin -d cinema -c "\dt"
```

### Проверка API документации
Откройте в браузере: http://localhost:8000/docs

## 🎯 Следующие шаги

1. **Исправить эндпоинты с ошибками**
2. **Добавить недостающие эндпоинты**
3. **Протестировать полный цикл работы**
4. **Добавить валидацию данных**

## 📝 Полезные команды

### Пересборка контейнеров
```bash
docker-compose build
docker-compose up -d
```

### Просмотр логов
```bash
docker-compose logs -f backend
```

### Остановка сервисов
```bash
docker-compose down
```

### Очистка и пересборка
```bash
docker-compose down -v
docker-compose build --no-cache
docker-compose up -d
```

## 🎉 Заключение

API частично работает! Основные функции доступны, но некоторые эндпоинты требуют доработки. Используйте файлы тестирования для проверки функциональности.

